{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://greenloom.github.io/cannabis-product-schema/v0.1/core/defs/vocab.schema.json",
  "title": "Core vocabularies",
  "description": "The two orthogonal type axes: product_form (physical form) and regulatory_class (compliance overlay). Both sets are deliberately the FULL representable universe, not any one jurisdiction's allowed subset — core is representable-everything, profiles restrict. A value a jurisdiction profile later excludes (e.g. flower or marijuana) is still valid core data.",
  "$defs": {
    "product_form": {
      "type": "string",
      "enum": [
        "flower",
        "preroll",
        "edible",
        "concentrate",
        "vape",
        "tincture",
        "topical",
        "beverage"
      ]
    },
    "regulatory_class": {
      "type": "string",
      "enum": ["hemp_derived", "delta8", "marijuana", "other_cannabis"]
    }
  }
}
